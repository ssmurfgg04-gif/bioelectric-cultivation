# SYNTHESIS — THE FINAL SESSION (exp302–304, closing exp288–304)

Date: 2026-09-21. HEAD at writing: `304f01a` (batch-58). Ledger: L1–L289.
This deposit evaluates the final experiments against the project's standing
results and closes the two remaining questions the last session left open.

## 1. What the final experiments showed

### exp302 — THE COMPOSED STRESS FACE (4/4, PROTECTED-PRESERVED)

The 2×2 factorial (the composed union × the write-time −35.0 pin) over
exp256's 72-row battery landed the session's headline: **the composed
carrier's protection is +13.1292 mV — ~18× the single ctx register's
+0.7285 mV (exp293)** — and the stress delta under the union is **+0.0000
mV**: the U1-S1 cell mean equals the U1-S0 cell mean to four decimals
(0.6189 mV). The write-time stress that degrades the schema-only walk by
13.13 mV (exp290's landed form, reproduced bit-exact as the U0-S1 anchor)
is *invisible at the decode* when the union carrier at the composed optimum
g=1.0 is present. The mechanism face: at g=1.0 the blend weight discards
the stressed commit value entirely at every armed site (boundary + junction
cells) and the sigma face kills the noise at the marked cells — the stress's
only remaining channel is the unarmed interior dynamics, and the decode
cannot see it. The ratio face (protection ÷ rest improvement) rises from
28.2× (the single register) to 37.0× (the union): **protection composes
super-additively, mirroring the improvement's own sub-saturation
composition (exp299) — the carrier's two faces move together.**

### exp303 — THE ZERO-SUBSTRATE 8TH FORMALIZATION: THE TRANSPORT FORM (4/4)

The 8th formalization class stated the coherence constraint on the
program's own output — the commit stream as a candidate pattern
representation, replayed into a fresh wiring-only substrate (no spec
install, no clamps, no encode, no carried state). Two results:

- **POSITIVE (new):** the stream is a *complete same-substrate
  representation* — 72/72 replays within the 6.0 mV bar at a mean
  region-scoped RMS of 0.5921 mV, essentially the walked decode's own
  quality. The write product alone, with nothing but the wiring, carries
  the walked region's pattern.
- **NEGATIVE (pre-named clause fired, then honestly repaired):** the
  transport face landed 2/132 — and the two in-bar pairs were exactly
  H0↔H1, whose substrates are *bit-identical* (the chain constructor's
  echo, asserted in every rebuild). The pre-named clause counted a
  degenerate pair; the ledger (L288) disclosed the hole without patching
  the gates, and exp304 repaired it pre-named.

### exp304 — THE DEGENERACY REPAIR + THE CARRIER'S STREAM (4/4, BOTH-BOUND)

The repair reproduced exp303's 130 genuine dormant transports **bit-exact
130/130** against the deposited per-pair RMS (the cross-deposit anchor) —
the re-scoping changed nothing but the pair set. The new science: the
**carrier's** stream (the union's re-assertion writes at g=1.0) reconstructs
its own substrate at **0.0548 mV — ~11× better than the dormant's** (the
armed values ARE the spec install, so the content face is near-exact,
12/12) — but transports **0/130** genuine pairs (the mean transport price
+16.28 mV, the stress-degradation scale). The carrier's stream is
wiring-bound exactly as the dormant's.

## 2. Did anything fundamentally change? YES, twice.

1. **The protection headline moved by an order of magnitude.** Before
   exp302 the standing protection result was the single register's
   +0.73 mV / ~29× ratio (exp293). After: the composed carrier protects
   at +13.13 mV and renders the write-time stress *decode-invisible*
   (+0.0000 mV). The carrier is not merely a read-side improvement — it
   is a **protective state variable under composition**, the strongest
   single engineering result in the ledger.
2. **The zero-substrate block gained its final, sharpest form.** The 8th
   formalization (the transport form) split the question into two faces
   that had never been separated: the *content* face (does the write
   product represent the pattern on its own wiring? — YES, near-exactly)
   and the *transport* face (does it represent the pattern on a different
   wiring? — NO, nowhere, in either form). The constraint survives, and
   the block is no longer just "the coherence constraint is robust": it
   is now located — **the pattern lives in the wiring-keyed indexation as
   much as in the values.** A representation exists only relative to its
   own wiring. This is a characterization, not just a refusal.

## 3. Earlier findings: confirmed, extended, not overturned

- **Confirmed bit-exact (anchors):** exp256 (the substituted battery),
  exp282 (the walk trajectories), exp287 (the commit layer — the streams
  ARE the deposited commit sequences, 72/72 digests), exp290 (the A1
  stress arm), exp300 (the union rows, 12/12 slice + 72/72 in exp302),
  exp303 (the 130 transports, bit-exact 130/130 in exp304's F1). Zero
  anchor failures across the session.
- **Extended:** exp293's protection form (single register → composed
  union, ~29× → ~37× ratio, +0.73 → +13.13 mV); exp290's stress
  degradation (13.13 mV — re-measured as the transport price's natural
  scale, ~15–16 mV, the same order); exp301's write-side object (promoted
  from a digest face to the 8th formalization's instrument); exp300's
  composed optimum (the saturation face is also the stress-optimal face).
- **Not overturned:** nothing. Every pre-existing deposit used as an
  anchor reproduced bit-exactly.

## 4. Does the reframed star still hold? YES.

Stage 5's reframed star — the universal reader (media × size axes closed,
n=5000) + the universal substrate (100 hosts, autocatalytic closed) —
stands untouched by the final session; every experiment consumed it as
the anchored machinery and none refuted a face of it. The history
register (the first non-inert channel) is now characterized end-to-end:
carried (exp296), dose-mapped (exp297), decomposed and composed
(exp298–300), write-side carried (exp301), protective under composition
(exp302), and its write product formalized (exp303/304).

## 5. Does the zero-substrate block still hold? YES — at 8 formalizations.

Blocked at 8: the five static metrics (exp68), the temporal schedule
(exp162), and the gauge quotient (exp244) were joined by the transport
form (exp303/304). The final form is the sharpest and the most useful:
the write product is a complete representation *relative to its own
wiring* and transports *nowhere*. The next researcher should not re-attack
the values; the binding is the indexation. Any future unblocking attempt
must make the *walk order* substrate-free (e.g., a wiring-independent
zone-addressing scheme), not improve the written values — the values are
already as good as they can be (0.05 mV under the carrier).

## 6. The session's discipline record

3 experiments (exp302, exp303, exp304), all pre-registered before their
bodies, all landed 4/4, all bodies written by the main agent, 26 deposits
touched READ-ONLY with zero drift, exp142 and the core never modified,
the floor restored to −60.0 at every exit, the full test suite green at
every landing, one pre-registration hole (exp303's degenerate-pair clause)
disclosed in-ledger and repaired by the next pre-registration rather than
patched in-gate. All work pushed: `3a4c45c` (exp302), `59d62b9` (exp303),
`304f01a` (exp304).
